<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do List</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body>

<div class="container">
    <h2>Things to Do</h2>
    
    <% if (!user) { %>
        <!-- Signup Form -->
        <div class="card p-3 mb-3">
            <h4>Sign Up</h4>
            <form action="/signup" method="POST">
                <input type="text" name="name" placeholder="Name" required class="form-control mb-2">
                <input type="email" name="email" placeholder="Email" required class="form-control mb-2">
                <input type="password" name="password" placeholder="Password" required class="form-control mb-2">
                <button type="submit" class="btn btn-primary w-100">Sign Up</button>
            </form>
        </div>
        
        <!-- Login Form -->
        <div class="card p-3">
            <h4>Login</h4>
            <form action="/login" method="POST">
                <input type="email" name="email" placeholder="Email" required class="form-control mb-2">
                <input type="password" name="password" placeholder="Password" required class="form-control mb-2">
                <button type="submit" class="btn btn-secondary w-100">Login</button>
            </form>
        </div>
    <% } else { %>
        
        <div class="welcome">Welcome, <%= user.name %>!</div>
        
        <!-- Add Task Form -->
        <form action="/add" method="POST" class="mt-3">
            <div class="input-group">
                <input type="text" name="description" class="form-control" placeholder="New Task" required>
                <button type="submit" class="btn btn-primary">Add</button>
            </div>
        </form>

        <!-- Task List -->
        <ul class="list-group mt-3">
            <% tasks.forEach(task => { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <span class="<%= task.completed ? 'text-decoration-line-through text-muted' : '' %>">
                        <%= task.description %>
                    </span>
                    <div>
                        <form action="/complete/<%= task._id %>" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-sm btn-success">✓</button>
                        </form>
                        <form action="/delete/<%= task._id %>" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-sm btn-danger">✗</button>
                        </form>
                    </div>
                </li>
            <% }); %>
        </ul>
        <br><br>
        <form action="/logout" method="POST">
            <button type="submit" class="btn btn-danger w-100">Logout</button>
        </form>

    <% } %>
</div>

<!-- Footer -->
<footer class="text-center mt-5 py-3 bg-light">
    <p>&copy; <%= new Date().getFullYear() %> Mpho I. All rights reserved.</p>
    <p>Email: <a href="mailto:mpho.itumeleng@icloud.com">mpho.itumeleng@icloud.com</a> | Phone: <a href="tel:0674058079">067 405 8079</a></p>
    <a href="https://github.com/MphoItumeleng" target="_blank" class="me-3">
        <img src="https://cdn.jsdelivr.net/npm/simple-icons@latest/icons/github.svg" width="24" height="24" alt="GitHub">
    </a>
    <a href="https://www.linkedin.com/in/mpho-itumeleng-202794295?utm_source=share&utm_campaign=share_via&utm_content=profile&utm_medium=ios_app" target="_blank">
        <img src="https://cdn.jsdelivr.net/npm/simple-icons@latest/icons/linkedin.svg" width="24" height="24" alt="LinkedIn">
    </a>
</footer>

</body>
</html>